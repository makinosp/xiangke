# Project Context (Hermes Override)

## 1. Base Rules

Follow all the standard project workflows and rules defined in `AGENTS.md`.
(Note to Agent: Please read `AGENTS.md` explicitly to ensure compliance with the
base workflow).

## 2. Rust Implementation Reference (High Priority)

When working on Rust implementation tasks (files matching `**/*.rs`):

- **Authoritative Source**: Always consult `.vendor/rust-reference/src/` for the
  official language specification.
- **Guideline**: Refer to `.ai-rules/rust-reference-guide.md` for specific
  implementation standards.
